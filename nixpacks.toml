[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake", "sqlite", "libsqlite3"]

[phases.install]
cmds = [
  "npm install",
  "npm rebuild better-sqlite3 --build-from-source"
]

[start]
cmd = "node server.js"